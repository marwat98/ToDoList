<section>
    <div class="display_input">
        <table id="toDoList_table" class="todo_table">
            <thead>
                <tr>
                    <th>Zrobione</th>
                    <th>Notatka</th>
                </tr>
            </thead>
            <tbody>
                {% for row in data %}
                <tr>
                    <td><button class="delete-btn" data-id="{{ row.id }}">Zrobione</button></td>
                    <td>{{ row.note }}</td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="4">Brak notatek</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</section>
